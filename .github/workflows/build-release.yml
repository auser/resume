name: Build and Release Resume

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Typst
        uses: typst-community/setup-typst@v4

      - name: Build PDFs
        run: |
          mkdir -p build
          typst compile resume.typ build/ari-lerner-resume.pdf
          typst compile resume-ats.typ build/ari-lerner-resume-ats.pdf

      - name: Generate release tag
        id: tag
        env:
          SHA: ${{ github.sha }}
        run: echo "tag=build-$(date -u +'%Y%m%d-%H%M%S')-${SHA::7}" >> "$GITHUB_OUTPUT"

      - name: Create Release
        uses: softprops/action-gh-release@v2
        env:
          COMMIT_SHA: ${{ github.sha }}
        with:
          tag_name: ${{ steps.tag.outputs.tag }}
          name: Resume ${{ steps.tag.outputs.tag }}
          body: |
            Auto-built from commit.

            - `ari-lerner-resume.pdf` — designed version, for humans.
            - `ari-lerner-resume-ats.pdf` — single-column, parser-safe version for
              application portals and automated screeners.
          files: |
            build/ari-lerner-resume.pdf
            build/ari-lerner-resume-ats.pdf
